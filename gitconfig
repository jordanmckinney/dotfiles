[user]
    name = Jordan McKinney
    email = jordanmmck@gmail.com
    username = jordanmmck
[push]
    default = simple
[alias]
    uncommit = reset --soft HEAD^
    unstage = reset
    discard = checkout -- .
    staged = diff --cached
    branches = branch -a
    remotes = remote -v
    lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
    lgg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n'' %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all
[core]
    editor = nvim
    excludesfile = ~/Development/DotFiles/gitignore_global
[merge]
    tool = vimdiff
[mergetool]
    path = nvim
[color]
    ui = auto
